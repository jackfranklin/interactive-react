<h3>Nested Routing</h3>

<ul>
  <p>It's annoying having to repeat the links on each route when really we want them on every page.</p>
  <p>React router lets us nest routes and components.</p>
  <p>Notice how we use <code>this.props.children</code> in nested routes.</p>
</ul>

<h3>Exercises</h3>
<ul>
  <li>Add a new page and a new link to that page.</li>
</ul>


